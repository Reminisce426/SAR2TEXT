# 2026-09-23 今日任务与审批单

状态：待用户审批。阶段1与阶段5均已完成本轮只读核查并回传，主任务完成汇总。本单不表示已经下载数据、运行描述/生成模型或开始训练。

## 今天完成什么

今天的核心成果是把第一轮数据、模型和试跑范围确定下来，为明天准备小样本提供依据。目录与Git准备已完成：桌面项目SAR2text、有效初始提交3427099；先前临时工作树创建已验证通过。

| 负责人 | 今天的任务 | 验收产物 |
|---|---|---|
| 阶段1｜研究任务与边界 | 复用已有调研，核对主数据来源、图文配对、小样本获取方式和剩余问题 | 一个首选数据方案、备选切换条件、待批范围 |
| 阶段5｜模型适配与双向任务 | 核对描述和文生SAR的首跑模型、权重、输入要求、许可及资源估计 | 每项一个首跑模型、必要替代、10–20例验收标准 |
| 主任务SAR2text_plan | 对照六周计划和现有配置，汇总两份回传 | 本审批单、依赖问题和明日动作 |
| 用户 | 阅读审批摘要，确认或修改首轮范围 | 对审批项的明确答复 |

阶段2的数据准备在选型审批后启动。其余阶段今天无需重复全面调研。

## 建议批准的最小范围

1. **数据**：BigEarthNet.txt文本与对应Sentinel-1影像作为首选候选；首先核对元数据、许可和图文映射，目标先获取20对可独立取得的样本，进行读取与人工对应检查（入口限制时允许8–32对）。通过后扩到约100–200条审计样本。建议本次数据/元数据总传输上限1GiB；超出或只能整包下载时回报原因与替代，不直接拉取整库。上限不包含模型权重，模型下载须先列清文件及总大小。
2. **模型**：复用已经加载的OpenCLIP作检索基线；描述首跑BLIP-base，文生SAR首跑sylviaHoch/SAR-StableDiffusion。首轮单卡batch=1推理，各保留10–20例与显存、耗时和失败日志；显存需求须实测。模型权重只获取运行所需文件，执行前核实体积及服务器空间。
3. **研究对照**：保存原始文本、事实证据与规范文本，规范化前后保持同等事实信息。第一轮考察描述是否准确和生成是否遵循条件，记录错误，不把输出格式更整齐直接解释为理解能力提高。

本轮审批通过后可以安排上述少量数据/必要权重获取与最小推理；不包含全量数据、全参数训练、付费服务、对外发布或已经批准论文主张。具体下载清单与模型权重大小仍应在执行前核对。

## 明天09-24的顺序

1. 阶段2根据批准选型确认样本入口及S1图文ID，生成样本清单；可只提供服务器运行命令，不假称已经接入DBCloud。
2. 检查极化、单位、dtype、NoData和已有处理链；只在正确理解产品后决定输入变换。
3. 人工检查约20例：描述事实是否能由SAR支持，是否含光学、地图或其他外部来源信息。
4. 用样本报告决定是否扩大到100–200条；划分按区域/源产品考虑，避免同源切片泄漏。

## 主任务已经确认的依赖

- 《阶段2_回传.md》记载：Sentinel2Cap许可表述存在冲突，BigEarthNet.txt对应S1能否按文件获取还没实测。这些仍需核验，不能写成已解决。
- `configs/openclip_vit_b32.json` 当前配置为 `/remote-home/sh/weights/ViT-B-32.pt`，用户此前成功加载路径为 `/remote-home/sh/models/openclip/open_clip_model.safetensors`。真实运行前由阶段3确认实际路径与模型激活参数，当前未擅自覆盖配置。
- manifest、image_root同样是模板路径；用户仅验证过OpenCLIP文本编码，不代表真实SAR图像、描述或生成模型已运行成功。
- 阶段1/5仍绑定旧cwd；本轮要求只读调研并在任务中回传，由主任务落盘到桌面目录。后续编码应使用正确主目录或同仓库工作树。

## 审批方式

阅读下面的核查汇总后，可回复“批准首轮小样本与单卡推理”，或写明希望调整的数据、模型、规模。用户决定前，状态保持待审批。建议今天只决定首轮范围；不把主候选锁定为最终论文数据集。

## 阶段回传与主任务复核

### 阶段1：数据和任务边界

事件：DAY1-APPROVAL-DATA-20260923。阶段1报告已核查官方页面：BigEarthNet.txt许可为CDLA-Permissive-1.0；s1_name可用于映射S1切片，实际配对尚未运行；S1官方整包约51GiB，逐文件取样待验证。Sentinel2Cap README与license.txt中的代码/数据许可表述冲突，暂缓作为本轮首选。体积及许可冲突为阶段1本轮核查回传，执行前再核对选定版本。

主任务复核了[BigEarthNet.txt官方页面](https://txt.bigearth.net/)：确有S1/S2配对、文本与空间标注，文本涉及参考地图和元数据，因此需要把外部辅助事实与SAR可观察证据分开；页面明确标示CDLA-Permissive-1.0。通过小样本后再决定是否扩规模。

### 阶段5：首跑模型

事件：DAY1-APPROVAL-MODEL-20260923。阶段5完成官方模型卡/接口核查；未下载或做GPU实测。主任务复核了以下模型页面。

- [BLIP-base官方权重](https://huggingface.co/Salesforce/blip-image-captioning-base)：作为通用图像描述起点，用SAR样本测试迁移表现；有输出只算流程跑通，需人工核对事实。SAR-CoCa保留为领域对照候选，许可及具体文件核验后再采用。
- [SAR-StableDiffusion官方模型卡](https://huggingface.co/sylviaHoch/SAR-StableDiffusion)：单通道VH合成振幅影像，许可标示CC BY-NC-SA 4.0。按作者正文加载文本编码器LoRA，不能仅复制页面顶部自动生成的通用示例。首轮测试地物类别条件；更复杂的比例/位置遵循属于待验证能力。输出不能直接称为真实物理GRD产品。
- 按单卡batch=1规划，优先FP16和必要CPU offload；4张显卡不能直接合并成单卡显存。超显存时先记录峰值、降低并发并启用合适节省显存策略；不为赶日程临时从头训练替代生成模型。

### 首轮验收

1. 数据：至少一批可重复读取且一一可追溯的图文对，记录极化、数值域、处理状态和外部标签来源。
2. 描述：10–20条原始模型输出及逐条事实核验，统计明显错误/无依据内容；不要先润色再评价。
3. 生成：10–20幅结果，保存提示词、随机种子、步数和模型版本，人工检查是否符合主要地物条件。小样本不作为显著性证明。
4. 每条运行路线都有可重复命令和资源日志；失败也交付最小复现。若模型与数据输入/输出数值域不同，先建立清楚的比较映射，不直接比较数值。

本轮阶段1/5均已完成审批材料；等待用户决定后，由主任务给阶段2/3/5分批安排具体执行，同一时刻最多两项独立实质工作。
