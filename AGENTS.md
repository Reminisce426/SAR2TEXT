# 项目协作规则

## 目录和工作树

- 主仓库和成果汇总目录：`C:\Users\LENOVO\Desktop\腾飞科创_SAR-自然语言标准`，Codex项目名 `SAR2text`。
- Mac 上的 GitHub 克隆目录是独立工作副本。先用 `git rev-parse --show-toplevel` 确认实际仓库根目录；下文 Windows 绝对路径只用于定位 Windows 主仓库，Mac 不应尝试访问。跨设备改动通过 Git 提交、推送和拉取交换，工作副本及 Codex 对话不会自动同步。
- 允许在该仓库创建的独立Git工作树中工作。写入前核对 `git rev-parse --show-toplevel`、`git rev-parse --git-common-dir` 和 `git status --short`，确认归属和现有改动。
- 工作树内提交后，由主任务审查并合并回主仓库；工作树间不自动同步。不要从工作树越过隔离直接改主目录的共享文件。
- `C:\Users\LENOVO\Documents\ChatGPT\腾飞科创_SAR-自然语言标准` 仅为历史副本，禁止写入。若任务仍绑定旧目录且无法在有效目录工作，应说明需在新项目继续，不回退写旧目录。

## 研究主线和日程

用户最新确定：SAR图像描述与自然语言生成SAR为主体，双向检索作为基线。通过图像、语言和转换流程的规范化，研究事实理解、表达和空间定位。以 `docs/项目总计划_两周汇报制.md` 的六周计划统筹，09-30、10-19、11-02汇报；10-05改为首次汇报后的内部复核节点。六阶段是职责分工，不是依次等待的六个周期。

阶段1候选数据/模型方案仍待用户审批；不得把紧凑排期当作已获下载、挂载或微调方案批准。计划、代码存在、静态检查、真实推理、训练及有效性结论必须明确区分。

## 分工和文件规则

1. 阶段1：研究问题、相关工作、选型与边界。
2. 阶段2：GRD数据、文本证据、划分与评价。
3. 阶段3：检索基线、运行接口与复现。
4. 阶段4：规范化、公平对照、消融和稳健性。
5. 阶段5：描述与文生SAR模型、轻量适配。
6. 阶段6：证据审查、两周汇报、最终交付。

- 大数据、权重、缓存及训练中间文件留在DBCloud；Git保存代码、配置、环境清单、manifest、小型指标/图表及报告。
- 改动前检查Git状态，保留其他任务的未提交修改。共享文件同一时刻只由一个任务维护。
- 实验记录数据与模型版本、配置、代码提交号、命令、输出目录和指标；随机训练/生成还须记录种子。
- 汇报材料保存在 `docs/reports/YYYY-MM-DD/`。服务器与本地尚未自动同步，完成传输后才能声称结果已回传。

## 每轮结束的轻量同步判定

用户授权每次实质工作结束前自动判断是否同步。仅根据本轮已有结果判断，不为判定增加检索、训练或代理。

- 阶段完成、经验证的关键结果、目标调整、阻塞、依赖或日程变化：通知主任务。
- 数据/Schema、接口、预处理、评价协议或结论影响其他阶段：只通知受影响任务。
- 普通小修、重复信息和收悉回执：不发通知。收到同步后不自动扩大工作；同一事件不循环转发。
- 消息约150–300字，包含事件ID、完成/变更、绝对路径或提交号、验证状态、影响及是否需行动。发送失败要在最终答复注明，不声称送达。
- 最终回复末尾简述同步去向或未同步原因。本规则通知本身不需要广播回执。

现有任务通讯录（重建任务后由主任务更新）：

- 主任务 SAR2text_plan：`01a0aa03-dba0-7291-9921-494dfbd9ae25`
- 阶段1：`01a0c7e3-eb18-7201-99ed-2aa5715e0116`
- 阶段2：`01a0c7e4-0800-77f1-aa9f-4e784768caaa`
- 阶段3：`01a0c7e4-1671-7b32-8142-c7d1a0ae91b9`
- 阶段4：`01a0c7e4-2a41-7090-b7ca-8fcda39926dc`
- 阶段5：`01a0c7e4-3974-7660-b6b1-d5813000fb55`
- 阶段6：`01a0c7e4-4f2c-7873-96ce-530c007c4c77`

## Codex使用

主任务统筹和验收；每次最多两个阶段任务执行独立实质工作。聚焦本轮相关文件和日志，复用已有调研。一般整理可选择轻量模型，复杂设计与调试用高能力模型；未获用户明确要求时不更改任务模型设置。额度按账号共享，保留约20%供排障和汇报。训练期间由DBCloud保存日志，避免持续唤醒任务轮询。
