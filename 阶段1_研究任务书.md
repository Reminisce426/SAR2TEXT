# 阶段 1 研究任务书（讨论稿）

**课题暂定名称：** 面向 Sentinel-1 GRD 的 SAR 图文语义对齐与规范化表达研究  
**阶段目标：** 明确任务边界、最低可行研究范围和首次沟通待决事项。本阶段不下载数据、不训练模型。  
**事实基线：** 已具备 4×RTX 2080 Ti（约 11GB/卡）、PyTorch 2.4.1+cu118、OpenCLIP 3.3.0；ViT-B/32 OpenAI 权重已离线校验并成功加载。正式数据、真实 SAR 推理、基线结果及对照实验均尚未完成。

## 一、三类任务必须分开定义

| 任务 | 输入 | 输出 | 研究目的 | 主要评价（同数据、同预算比较） |
|---|---|---|---|---|
| **双向图文检索** | 图搜文：SAR 图像；文搜图：自然语言查询 | 候选文本或图像的排序列表 | 学习并检验 SAR—文本共享语义空间；“双向”指图→文与文→图两条检索方向 | Recall@K、mAP、Median/Mean Rank；两方向分别报告，并给出均值；重复运行报告均值±标准差 |
| **SAR 图像描述** | 单幅 SAR 图像及必要元数据 | 一条或多条自然语言描述 | 将散斑、后向散射、地物和成像条件转成可读、可控的规范表达 | BLEU、METEOR、ROUGE-L、CIDEr 等文本指标；另做人评或结构化字段准确率，防止“语言通顺但事实错误” |
| **文字生成 SAR** | 自然语言描述，可附成像模式、极化、分辨率等条件 | 合成 SAR 图像 | 检验文字条件对 SAR 外观或场景生成的可控性 | FID/KID 等分布指标、图文一致性、条件遵循率、下游任务一致性及专家核验；不能只用自然图像审美判断 |

**边界结论：** 三类任务的数据组织、模型、成本和评价不同，不能合并称为一个“双向模型”。当前暂定“双向”仅指双向图文检索；图像描述和文字生成 SAR 均作为待确认扩展方向。

## 二、最低可行目标与扩展目标

### 最低可行目标（建议作为当前主线）

1. 以 **Sentinel-1 GRD—文本配对数据上的双向图文检索** 为唯一主任务，先建立可复现基线。
2. 将“规范化表达”定义为可复现的文本/元数据转换流程，例如术语统一、字段顺序统一、单位和极化表示统一、可核验信息保留；不预设其一定有效。
3. 固定模型、数据划分、训练预算和评价协议，对“原始表达—规范化表达—去除单项规范规则”的对照与消融进行检验。
4. OpenCLIP、SAR-CLIP、SAR-TEXT 仅作为可引用的基线或参考；本课题的贡献候选是**规范规则、可追溯转换流程及其实验证据**，最终创新性须在文献核查和实验后再判断。
5. 最低交付：任务与数据协议、可复现基线、规范化流程、对照/消融结果、失败案例分析和复现实验说明。

### 扩展目标（满足前置条件后只选其一）

- **扩展 A：SAR 图像描述。** 前提是存在足量、可信、可核验的描述标注，且检索主线已稳定；优先生成受控字段或模板化描述，再评估自由文本。
- **扩展 B：文字生成 SAR。** 前提是学长明确要求、获得适配数据与额外算力/周期，并先定义 SAR 真实性与条件一致性评价。按当前 4×11GB 资源，不建议作为最低目标。
- **扩展 C：跨区域、跨季节或跨成像条件的稳健性。** 若数据允许，优先于直接增加生成任务，更贴合“准确性、稳定性、可复现性”的主线。

## 三、需向学长确认的问题与未答前暂定方案

| 必须确认的问题 | 未回答前的暂定方案 |
|---|---|
| 1. 题目中的“双向”究竟指图搜文+文搜图，还是还要求图像描述/文生 SAR？ | 仅定义为双向检索；其他两项不纳入承诺成果 |
| 2. 最终必须完成的主任务、扩展任务和验收成果是什么？ | 主任务只做双向检索；扩展任务最多择一，且需单独评审 |
| 3. 指定数据集或应用场景是什么？是否必须限定 Sentinel-1 GRD？ | 限定 Sentinel-1 GRD；先选公开、许可清楚、含文本或可追溯标签的数据 |
| 4. 文本来自人工描述、类别标签扩写、元数据模板，还是大模型生成？ | 保留来源字段；人工、模板和模型生成文本不得混作同一真值 |
| 5. “规范化”重点是术语、句式、元数据、标签体系还是处理流程？ | 采用可逆/可追踪的规则化文本与元数据表示，逐项消融验证 |
| 6. 是否已有课题组代码、数据、评价协议或必须复现的论文？ | 以公开 OpenCLIP 为首个基线；其他工作仅在代码与许可可获得时复现 |
| 7. 对创新、论文/专利/系统演示及时间节点有何硬性要求？ | 先按每 1–2 周一次可审查增量推进，不承诺论文等级或性能提升 |
| 8. 是否允许使用外部预训练模型、机器生成文本和生成式模型？ | 允许作基线或辅助，但记录来源、版本、提示词和人工修改，不把外部能力当自有创新 |

## 四、第一次沟通提纲（建议 20–30 分钟）

1. **2 分钟：复述当前理解。** 研究对象为 Sentinel-1 GRD 图像、自然语言及二者之间的规范化转换；所有效果均以受控实验验证。
2. **5 分钟：展示三任务边界。** 重点确认“双向”是否仅指图搜文/文搜图，明确描述与生成不是同一任务。
3. **5 分钟：提出最低可行主线。** 说明为什么先做双向检索：评价清楚、可建立基线、适合现有显存，也能直接检验规范化是否改善对齐。
4. **8 分钟：逐项确认上述 8 个问题。** 优先拿到任务范围、数据来源、文本来源、验收成果四个决定。
5. **5 分钟：确认下一阶段入口。** 仅在主任务和数据边界确认后进入“数据与评价”；记录学长原话、决定、待办人和截止时间。

**本次沟通希望形成的书面结论：** 一句话任务定义；主任务/扩展任务清单；指定或允许的数据范围；文本真值来源；主要评价指标；阶段性成果形式与最近一次汇报日期。

> 注：以上除“事实基线”外均为当前研究假设或建议，不代表已经获得数据、完成实验或证明规范化有效。
