# 阶段 3：可复现检索基线与 SAR 专项基线审计

## 本阶段确定的结论

1. 通用基线固定为离线 OpenCLIP ViT-B/32，仅执行文字找图与图找文；图像描述和文字生成 SAR 属于其他任务，不并入本阶段 Recall@K。
2. 统一检索协议采用 `image_id` / `caption_id` 显式关系，支持一图多文本；文字找图以对应图像为正例，图找文以该图像的全部描述为正例。
3. 候选库按测试清单筛选后首次出现顺序固定；相同相似度按候选索引升序稳定打破并列。所有对照模型必须共享清单、划分、正例映射和指标实现。
4. 当前输入约定为 Pillow 读取后转 RGB；单通道 SAR 复制为三通道。这只是通用基线处理，正式实验需与后续 SAR 规范化方案做对照。
5. CAESAR-Radi SARCLIP 与 SAR-TEXT 项目的 SAR-RS-CLIP 是不同对象。前者代码 MIT，SARCAP 仅限非商业研究/教育并受第三方数据条款约束；后者仓库代码 Apache-2.0，但未找到数据与权重的独立许可，许可澄清前不应进入可发布实验。
6. 目前没有正式数据、SAR 图像推理或基线数值；不得引用论文数值冒充本项目复算结果。

## 已完成成果及文件位置

- 项目说明与运行协议：`README.md`
- 离线 OpenCLIP 配置模板：`configs/openclip_vit_b32.json`
- 可复现检索代码：`src/sar_baseline/`
  - 清单读取与约束：`manifest.py`
  - 双向 Recall 参考实现：`metrics.py`
  - OpenCLIP 离线编码器：`encoder.py`
  - 批量评估与案例导出：`evaluation.py`
  - 随机种子、环境快照、SHA256：`reproducibility.py`
  - 清单校验入口：`validate_manifest.py`
  - 完整运行入口：`run.py`
- SAR 专项基线审计：`docs/sar_baseline_audit.md`
- 最小测试：`tests/test_metrics.py`、`tests/test_manifest.py`
- Python 包与依赖声明：`pyproject.toml`

## 验证结果

- `python -m unittest discover -s tests -v`：5 项全部通过。
- `python -m compileall -q src tests`：通过。
- `git diff --check`：通过。
- 未验证项：真实 OpenCLIP 权重加载、GPU 推理、SAR 图像读取和正式数据指标。当前本地 Python 环境无 PyTorch，正式环境与数据也未在本轮启用。

## 仍未解决的问题

1. 正式数据集、测试划分和自然语言来源尚未确定，因而无法冻结最终 manifest。
2. 现有 ViT-B/32 离线权重的实际路径与 SHA256 需要在 DBCloud 恢复测试时填入配置。
3. 正式 SAR 数据的位深、动态范围、极化通道、GeoTIFF 支持与可视化映射尚未定义；Pillow RGB 只适用于第一版通用基线。
4. CAESAR-Radi SARCLIP 和 SAR-TEXT 权重均未下载；其文件哈希、结构兼容性、依赖可安装性和复算指标尚未验证。
5. SAR-TEXT 数据与权重缺少独立许可说明，需要作者/发布页澄清；还需检查训练数据与最终测试集来源重叠。

## 是否需要修改研究主线

暂不需要。继续坚持“先用通用 OpenCLIP 建立可复现检索基线，再在同一协议下评价规范化和 SAR 专用模型”的主线。若最终目标转为图像描述或文字生成 SAR，应另建任务定义、数据和评价体系，不应直接扩写本检索基线。

## 建议下一步

1. 数据确定后先生成 8–32 个样本的临时 manifest，在 DBCloud 完成离线权重、图像读取和单 GPU 冒烟测试。
2. 冒烟测试通过后冻结正式测试 manifest 及 SHA256，运行 OpenCLIP 双向检索并保存全部运行产物。
3. 人工检查最佳/最差案例，确认错误来自语义、SAR 成像差异还是描述规范问题。
4. 只在许可、权重、依赖和数据重叠审计完成后接入一个 SAR 专用基线；仍用本仓库统一指标复算。
