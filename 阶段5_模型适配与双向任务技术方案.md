# 阶段 5：模型适配与最终“双向任务”技术方案

> 课题暂定：面向 Sentinel-1 GRD 的 SAR 图文语义对齐与规范化表达研究  
> 文档状态：待主流程确认任务边界与正式数据后执行  
> 本阶段性质：技术方案与边界设计，不包含训练结果

## 1. 本阶段目标与决策原则

本阶段不把“图文检索、SAR 图像描述、文字生成 SAR”合并成一个含混的“双向”任务，而是分别定义输入、输出、模型、评价和资源需求。建议主线仍以图文检索建立可复现基线；图像描述作为数据质量允许时的扩展；文字生成 SAR 仅在数据规模、标签条件和研究周期足够时立项。

共同原则如下。

1. OpenCLIP、RemoteCLIP、SARCLIP、SAR-TEXT 中的模型只作为公开基线、初始化或路线参考，不写成自己的创新。
2. 首先在单张 RTX 2080 Ti（约 11 GB）上验证数据管线、前向/反向、显存和指标；单卡闭环通过后再使用 4 卡。
3. RTX 2080 Ti 不以 BF16 为默认精度，统一从 FP16 自动混合精度起步；所有显存数字只能在正式分辨率、批量和代码确定后实测记录。
4. 所有比较固定数据划分、随机种子集合、预处理、模型初始化、训练步数或样本访问量和超参数搜索预算。
5. “规范化是否有效”和“模型是否因微调变强”必须用因子实验分开回答。
6. Sentinel-1 GRD 是检测后的地距产品，不包含可恢复的复数相位。灰度显示图、定量后向散射数据和原始复数 SAR 数据不能混称。

## 2. 三条任务路线总览

| 任务 | 输入 → 输出 | 最小可行路线 | 4×2080 Ti 可行性 | 当前建议 |
|---|---|---|---|---|
| 图文检索 | SAR 图像 ↔ 文本 | OpenCLIP ViT-B/32 冻结基线 → 投影层/Adapter/LoRA | 高 | 主线，优先完成 |
| SAR 图像描述 | SAR 图像 → 受控文本 | 冻结视觉编码器 + 小型跨模态桥接层 + 小型自回归解码器 | 中；依赖可靠 caption | 扩展任务，先做小规模可学性核查 |
| 文字生成 SAR | 文本（及必要元数据）→ SAR 表达 | 256×256 条件扩散；只微调 LoRA/Adapter | 中低；数据和评价门槛高 | 不作为默认主线，需单独立项门槛 |

“双向”的推荐定义为同一对齐空间中的 **图到文检索（I2T）与文到图检索（T2I）**。它们天然共享模型与评价协议，最适合当前算力和研究进度。图像描述和文字生成图像是不同的生成任务，不应作为“检索双向”的反向过程。

## 3. 路线 A：SAR 图文双向检索

### 3.1 任务定义

- I2T：给定一幅 SAR 图像，从候选文本库中找出匹配描述。
- T2I：给定一条文本，从候选图像库中找出匹配图像。
- 正样本定义必须与数据来源一致：若一图多文，则该图对应的全部合法描述均为正样本；不能把同图的其他描述算作负样本。
- 同地点、同事件、相邻切片、同源增广样本必须按组划分，防止训练/测试泄漏。

### 3.2 基线与适配顺序

按由低成本到高成本的顺序推进，任何后一级都必须与前一级在同一测试集比较。

1. **零训练基线**：离线 OpenAI CLIP ViT-B/32；灰度图采用固定三通道复制或经主流程确认的多极化映射；文本不做任务特定训练。
2. **线性投影头**：冻结图像和文本编码器，在归一化前分别加入小型线性层或两层 MLP，将特征映射到共同维度；首先只训练投影头和 logit scale。
3. **Adapter**：冻结主干，在视觉和文本 Transformer 的后若干层加入瓶颈 Adapter；先视觉单支路，再双支路。
4. **LoRA**：冻结主干，在后 4 层注意力的 `q_proj`、`v_proj` 起步；确认不足后再扩大到 12 层或加入输出投影。推荐初始 rank `r=4/8`，缩放系数 `alpha=8/16`，dropout `0.05`。
5. **部分解冻**：仅在 LoRA/Adapter 已达到稳定上限、数据量足够且过拟合可控时，解冻视觉或文本编码器最后 1–2 个 block；不直接全量微调。

适配方案之间不是简单叠加。主比较建议选“投影头、Adapter、LoRA”三种参数高效方法；组合方案只能作为后续补充，否则难以解释收益来源。

### 3.3 单卡起步配置

以下是起始配置，不是已经实测的上限。

| 项目 | 建议起点 |
|---|---|
| 图像尺寸 | 与 ViT-B/32 权重一致，从 224×224 起步 |
| 文本长度 | 77 tokens 以内；规范文本若超长应先定义截断规则 |
| 精度 | FP16 AMP；指标计算和特征归一化可保留 FP32 |
| 单卡 batch | 32 起测；依次试 16/32/64，记录峰值显存 |
| 有效 batch | 128 左右起步，用梯度累积实现 |
| 优化器 | AdamW；只向可训练参数注册 optimizer state |
| 训练对象 | 第一轮仅投影头；第二轮再试 Adapter 或 LoRA |
| 防护 | 梯度裁剪、NaN 检查、固定 seed、保存数据清单和配置 |

CLIP 的 in-batch negatives 受 batch 大小影响。若各卡局部 batch 很小，多卡训练需要显式确认是否跨卡聚合特征；否则“4 卡训练”与“单卡大 batch”并不等价。跨卡聚合的正样本索引也必须相应偏移。

### 3.4 参数高效方案的量级

以 ViT-B/32 为例，以下只说明数量级，最终以代码统计 `trainable_params` 为准。

- **双 512 维投影头**：若每支路为 `512→512` 线性层，合计约 0.53M 参数；两层 MLP 会略增。
- **瓶颈 Adapter**：若视觉宽度 768、文本宽度 512，每层放一个 `d→64→d` Adapter，覆盖两侧 12 层，约 2.0M 参数；若每个 block 放两组 Adapter，数量约翻倍。
- **LoRA**：若两侧 12 层的 Q、V 都用 `r=8`，约 0.49M 低秩参数；只放后 4 层则约为其三分之一。

冻结参数本身仍占模型权重和前向激活显存。投影头方案最省显存；内部 Adapter/LoRA 需要梯度穿过主干，必要时启用梯度检查点。

### 3.5 评价

必须同时报告 I2T 与 T2I：

- Recall@1、Recall@5、Recall@10；
- mean Recall（明确是六项平均还是两个方向各自平均）；
- Median Rank、Mean Rank；
- 至少 3 个随机种子的均值和标准差；
- 按场景类别、极化方式、分辨率/尺度、文本长度或规范字段完整度分层报告；
- 记录推理吞吐、单样本时延、可训练参数量和峰值显存。

检索库规模会直接影响 Recall@K，所有模型必须使用完全相同的候选库。若文本是由模板从标签生成，需增加“同义改写/未见模板”测试，避免模型只记住模板词。

## 4. 路线 B：SAR 图像描述

### 4.1 任务边界

图像描述的目标不是生成“听起来合理”的遥感句子，而是从图像证据中生成可核查、低幻觉、格式一致的描述。必须先明确 caption 的信息层级：

- **可见层**：散射强弱、纹理、形状、数量、空间关系；
- **语义层**：水体、建筑、船只、机场等有可靠标注的对象或场景；
- **传感器/元数据层**：VV/VH、轨道、入射角、时间等只能从元数据读取，不能要求模型从像素“猜”；
- **推断层**：灾害程度、目标材质或因果结论若无独立依据，禁止作为必答字段。

建议输出“受控字段 + 自然语言表述”两部分。前者便于精确评价，后者检验表达质量。例如先预测对象、数量区间、空间关系、散射等级和不确定性，再按固定文法生成描述。

### 4.2 模型路线

推荐从轻到重：

1. **检索式描述基线**：用检索模型找最相似训练图像，返回其 caption。该基线可以揭示生成模型是否只是在复述近邻。
2. **冻结视觉编码器 + 小型 Transformer 解码器**：ViT-B/32 输出视觉 tokens，经 2–4 层跨模态桥接层输入解码器；解码器从约 100M–500M 参数量级起步。
3. **LoRA 解码器 + 视觉 Adapter**：主干冻结，在解码器注意力与视觉后 4 层加入 LoRA；视觉/语言两侧不得同时大规模解冻。
4. **CoCa/SAR-RS-CoCa 路线复现**：只有在 SAR-TEXT 数据许可、代码和权重可获得且前两级已闭环时作为强基线，不直接当作自研模型。

不建议在 4×11 GB 上从头训练大型多模态语言模型，也不建议一开始使用 7B 级模型。量化 LoRA 可作为后续工程选项，但应先确认当前 CUDA/PyTorch 环境下相关依赖能稳定运行。

### 4.3 资源起点

- 分辨率 224×224；caption 最大 64 tokens；单卡 batch 4–8 起测，梯度累积到有效 batch 32–64。
- 冻结视觉编码器；训练桥接层和小型解码器或其 LoRA；FP16 AMP + 梯度检查点。
- 若单卡峰值显存超过约 10 GB，优先减 batch、缩短序列、减少桥接层，再考虑 8-bit optimizer/量化；不以 OOM 后立即上多卡来掩盖配置问题。
- 单卡先对 500–2,000 个样本做过拟合测试：训练损失应下降，受控字段应可记忆；这只验证管线可学，不作为泛化结果。

### 4.4 独立评价

自动语言指标只反映表面重合，不能单独证明 SAR 语义正确。建议四组指标：

1. **文本重合**：BLEU-4、ROUGE-L、METEOR、CIDEr、SPICE；
2. **语义一致**：用固定的检索编码器计算 image-caption 相似度，或做 caption→image 回检索；评价编码器必须与训练模型区分开并固定版本；
3. **结构化准确性**：对象类别 F1、数量区间准确率、空间关系 F1、字段完整率、格式合法率；
4. **幻觉与人工盲评**：不存在对象的幻觉率、元数据臆测率、事实正确性、信息完整性和可读性。人工评价应双人独立并报告一致性。

## 5. 路线 C：文字生成 SAR

### 5.1 必须先选择的输出定义

#### 方案 C1：视觉灰度图（建议的最低风险定义）

- 输出是经过固定对数变换、裁剪与拉伸后的 1 通道灰度可视化，或复制成 3 通道供现有生成模型使用。
- 它只追求视觉形态和文字语义一致，不声称像素具有可定量解释的 VV/VH 后向散射意义。
- 论文中应使用“文本条件 SAR 视觉模拟图/显示图”，不能写“生成真实 Sentinel-1 测量数据”。

#### 方案 C2：具有 VV/VH 物理意义的栅格（高门槛）

- 输出至少为分别定义的 VV、VH 两通道，并明确是幅度、功率、`sigma0`、`gamma0` 还是 dB；禁止混用。
- 条件输入除文本外，至少应包括极化组合、入射角或其范围、轨道方向、地面采样间距/目标分辨率、校准与地形校正流程；地理定位若保留还需投影和栅格变换。
- 训练目标和反归一化必须可恢复到指定数值域；不能只保存 8-bit PNG 后再宣称物理定量有效。
- 文本对像素场是严重欠定的，因此输出只能解释为“给定条件下的统计样本”，不能解释为某时某地的确定观测。

当前建议默认选 C1。只有主流程确认拥有足量、同处理链的双极化定量数据和元数据，且研究目标确实需要物理量，才进入 C2。

### 5.2 模型路线与算力约束

1. **非生成基线**：文本检索最匹配的真实 SAR 图像。它同时是生成质量的下限参照和防止训练样本复制的检查工具。
2. **256×256 条件 DDPM/LDM 小模型**：冻结文本编码器，训练较小 UNet 或只训练跨注意力/LoRA；先做单类别或少类别可学性验证。
3. **预训练扩散模型 LoRA**：仅适用于 C1 视觉图，且要明确自然图像预训练与 SAR 域之间存在模态差异；LoRA 应优先放在 cross-attention，`r=4/8` 起步。
4. **C2 物理栅格模型**：需要原生 2 通道编码/解码器、元数据条件编码和定量损失；不能简单把 Stable Diffusion 的 RGB VAE 输出当作 VV/VH。

单卡起点：256×256、batch 1、FP16、梯度累积、梯度检查点、冻结文本编码器；确认一个小 batch 能完成前向/反向并保存样本后，再试 batch 2/4。4 卡主要用于数据并行提高吞吐，不应在单卡不能闭环时直接启用。

### 5.3 C1 视觉图评价

- 文本—图像语义一致性：固定外部检索器的相似度、生成图→文本/文本→生成图检索；
- 分布相似性：在固定的 SAR 专用或遥感特征空间计算 FID/KID；不宜只用自然图像 Inception 特征；
- 多样性与覆盖：同一文本多次采样的 LPIPS/特征距离、类别覆盖、模式坍塌率；
- SAR 视觉统计：强度直方图、局部均值/方差、等效视数代理、功率谱、纹理统计、边缘/目标形态；
- 记忆检查：与训练集最近邻的像素和特征距离；
- 人工盲评：语义吻合、SAR 外观可信度和明显伪影，不能把主观“像”替代定量证据。

### 5.4 C2 定量 VV/VH 评价

除 C1 的语义和多样性评价外，必须单独报告：

- 每极化通道的线性域和 dB 域分布差异、均值/方差/分位数误差；
- VV/VH 比值或差值的分布与空间结构；
- 不同地物/场景下的后向散射条件分布；
- 与入射角、轨道方向等条件的一致性；
- 空间自相关、功率谱、纹理和斑点统计；
- 用生成数据训练、真实测试的下游效用，以及只用真实数据的严格对照；
- 校准、地形校正、裁剪、归一化和反归一化的全链路误差检查。

单个 FID、视觉样例或下游准确率都不能证明生成栅格具有物理真实性。

## 6. 分离“规范化收益”与“模型微调收益”

### 6.1 核心 2×2 因子实验

定义两个因素：

- `N`：是否使用已冻结版本的图像/文本规范化流程；
- `F`：模型是否进行参数高效微调。

| 组别 | 规范化 N | 微调 F | 作用 |
|---|---:|---:|---|
| A | 0 | 0 | 原始数据 + 冻结模型，最低基线 |
| B | 1 | 0 | 仅规范化，估计规范化直接收益 |
| C | 0 | 1 | 仅微调，估计模型适配收益 |
| D | 1 | 1 | 规范化 + 微调，检验组合与交互 |

对同一指标 `M`，报告：

- 冻结模型下的规范化收益：`M_B - M_A`；
- 原始数据下的微调收益：`M_C - M_A`；
- 微调模型下的规范化增益：`M_D - M_C`；
- 规范数据上的微调增益：`M_D - M_B`；
- 交互项：`M_D - M_C - M_B + M_A`。

交互项不为零表示规范化与微调不是简单相加。最终不能只比较 D 与 A，否则无法判断提升来自哪个因素。

### 6.2 公平比较约束

1. A/B/C/D 使用完全相同的数据对、训练/验证/测试分组和候选检索库。
2. 规范化规则只能在训练集上设计或拟合，并固定版本；测试集不能用于反复修改规则。
3. C 与 D 使用同一种适配模块、可训练参数量、初始化、训练步数、有效 batch、优化器和学习率搜索预算。
4. A 与 B 不训练主干；如果投影维度发生变化，必须另设“投影头训练”因素，不能暗中并入规范化组。
5. 每组至少 3 个随机种子；报告均值、标准差和配对差异的置信区间，样本级检索差异可用 bootstrap。
6. 除总指标外，按文本是否被改写、字段完整度、场景类别、极化/处理链等分层，检查规范化是否只改善某一子群。
7. 保留原始输入、规范化输入、规则版本、样本 ID 和失败原因，支持逐样本追踪。

### 6.3 规范化内部消融

主 2×2 实验确认有收益后，再逐项加入规范规则，例如：

1. 术语与中英文同义词统一；
2. 对象、数量、空间关系的固定槽位；
3. 传感器元数据与可见语义分离；
4. 不确定性或不可判定表达；
5. 图像动态范围、通道映射、尺寸和裁剪规则。

每次只改一个模块，并保持训练预算不变。若一次性改变文本、图像和模型，无法归因。

## 7. 单卡验证到 4 卡训练的门控流程

### Gate 0：静态检查

- 数据读取不修改原文件；样本 ID、配对关系、组划分和文件哈希可追踪；
- 输出一批原始图、模型输入图、原始文本和规范文本供人工核查；
- 统计可训练参数、总参数、输入形状和版本信息。

### Gate 1：单卡烟雾测试

- 8–32 个样本完成前向、反向、optimizer step、保存与恢复；
- 无 NaN/Inf；正样本索引正确；峰值显存低于安全线；
- 同一 checkpoint 重新加载后，固定输入的输出在容差内一致。

### Gate 2：小样本过拟合

- 在 100–1,000 对数据上证明损失可下降、训练检索率明显提高；
- 检查是否出现标签错位、重复样本或只记模板关键词；
- 生成任务还需检查是否复制训练图或产生固定句式。

### Gate 3：单卡小规模基线

- 固定一个小型正式子集，跑完 A/B/C/D 的最小版本；
- 得到首份包含指标、显存、耗时、seed 和配置的报告；
- 此处结果只作工程与趋势判断，不作为最终论文结果。

### Gate 4：4 卡扩展

- DDP 前后在同一有效 batch 下做短程等价性检查；
- 确认跨卡负样本、sampler 去重、随机种子和断点恢复；
- 再扩大数据规模、种子数和消融，不改变已冻结的测试集。

## 8. 资源估计与停止条件

### 8.1 存储与日志

持久化目录建议在 `/remote-home/sh` 下分为：`datasets/`、`checkpoints/`、`runs/`、`reports/` 和 `cache/`。每次运行保存：

- 完整配置、git commit 或代码快照、环境导出、随机种子；
- 数据 manifest 和 split hash；
- 每 epoch 指标、峰值显存、墙钟时间、最佳与最后 checkpoint；
- 失败运行的错误和最后一批样本 ID。

不要仅估计 checkpoint 大小；数据缓存、优化器状态、日志、生成样本和多 seed 结果往往占用更多空间。正式运行前按一个完整小实验的实际占用外推，并预留至少 30% 空间。

### 8.2 建议停止/降级条件

- 数据无法形成无泄漏的训练/测试划分：暂停模型比较，先修数据协议。
- caption 大量来自未验证自动生成或只由类别模板组成：图像描述不进入主结论，只做弱监督探索。
- 检索冻结基线尚未可靠复现：不启动 caption 或 diffusion。
- 单卡无法稳定完成一个 optimizer step：缩小模型/分辨率/序列，不直接多卡扩容。
- C1 生成模型未超过真实图检索基线、出现训练样本复制或无法通过独立 SAR 统计：停止扩大训练。
- 无定量 VV/VH、处理链和元数据：禁止进入 C2 物理栅格表述。

## 9. 待主流程确认的输入

在训练前需要主流程明确：

1. 最终主任务是否固定为 I2T/T2I 检索；caption 和生成是否列为扩展；
2. 正式数据集名称、许可、样本规模、图文来源、一图多文关系和训练/测试划分单位；
3. Sentinel-1 产品处理级别、极化方式、数值域、校准/地形校正流程和输出分辨率；
4. 研究语言是中文、英文还是双语；规范化目标是受控自然语言、结构化字段还是两者并存；
5. 可接受的公开预训练权重、许可证与引用方式；
6. 最终周期、存储上限以及可用于多 seed/消融的 GPU 时长；
7. 若要求文字生成 SAR，选择 C1 视觉灰度图还是 C2 物理栅格，并接受对应的评价成本。

## 10. 主流程回传

**阶段：**  
阶段 5——模型适配及最终双向任务技术方案与边界。

**本阶段确定的结论：**  
“双向”优先定义为同一对齐模型上的图到文检索和文到图检索；图像描述与文字生成 SAR 是两条独立生成任务。4×RTX 2080 Ti 适合以 ViT-B/32 为基础开展投影层、Adapter、LoRA 等参数高效适配，并必须单卡闭环后再多卡扩展。文字生图默认仅定义为 SAR 视觉灰度模拟；若声称 VV/VH 物理意义，必须生成明确数值域的双通道栅格、加入传感器条件并采用独立物理评价。

**已完成成果及文件位置：**  
`阶段5_模型适配与双向任务技术方案.md`。文档包含三条任务路线、显存与训练起点、生成输出边界、独立评价、2×2 收益分解和单卡到多卡门控。

**验证结果：**  
本轮为方案设计，未启动训练，因而没有模型性能结果。已从架构参数量级和 11 GB 单卡约束检查投影层、Adapter、LoRA 的工程可行性；真实 batch 上限、峰值显存和速度仍需在正式数据与代码确定后实测。

**仍未解决的问题：**  
正式数据集与许可、文本来源与质量、一图多文正样本定义、Sentinel-1 GRD 处理链、语言范围、最终双向任务定义、生成输出 C1/C2 选择、训练周期和存储预算尚未由主流程确认。

**是否需要修改研究主线：**  
暂不修改“规范化 + 可控实验”的总主线；建议把图文检索固定为第一主任务，并把 caption/文字生图设为需通过数据和资源门槛的扩展任务。若现有 caption 主要是模板或自动生成且无法人工抽检，则需下调生成任务的论文权重。

**建议下一步：**  
主流程先冻结任务、数据、划分和规范规则版本；随后在单卡上运行零训练 OpenCLIP 基线与投影头烟雾测试，记录实际显存、速度和 I2T/T2I 指标，再按 A/B/C/D 四组完成最小因子实验。只有该闭环通过后，才选择 Adapter 或 LoRA 扩展，并决定是否进入图像描述或文字生成 SAR。

## 11. 公开基线参考

- Sentinel-1 GRD 产品定义：Copernicus SentiWiki 明确说明 GRD 经检测、多视和地距投影，像素代表检测幅度且相位信息丢失：<https://sentiwiki.copernicus.eu/web/s1-processing>。
- OpenCLIP：现有离线 ViT-B/32 OpenAI 权重和本地 OpenCLIP 3.3.0 环境，作为第一可复现基线；官方实现与训练说明：<https://github.com/mlfoundations/open_clip>。
- RemoteCLIP：遥感图文双编码检索基线，官方仓库提供 OpenCLIP 兼容权重与推理示例：<https://github.com/ChenDelong1999/RemoteCLIP>。
- SAR-TEXT：包含 SAR 图文数据和检索、caption、VQA 路线，作为任务与强基线参考：<https://github.com/YiguoHe/SAR-TEXT>。
- SARCLIP：公开论文中的 SAR 视觉语言对齐路线：<https://arxiv.org/abs/2510.22665>；代码与数据可用性需在正式实验前重新核验，不能仅据论文描述假定可复现。
- Qosja 等人的 SAR diffusion 工作：<https://arxiv.org/abs/2405.07776>；只用于说明 SAR 条件/无条件扩散已有研究，不代表其数据和结果可直接迁移到 Sentinel-1 GRD。
