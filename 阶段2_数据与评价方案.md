# 阶段 2：公开数据筛选、划分与评价方案

核验日期：2026-09-22  
适用课题：面向 Sentinel-1 GRD 的 SAR 图文语义对齐与规范化表达研究  
当前主任务：SAR 图文双向检索；图像描述、文字生成 SAR 不在本阶段评价范围内

## 1. 结论先行

1. **原始 Sentinel-1 Level-1 GRD、由 GRD 派生的分析就绪影像、SAR—文本数据必须分层管理。** Level-1 GRD 是已检测、多视、投影到地距的产品，但仍不能等同于经过辐射定标、DEM 地形校正并转成 dB 的训练切块。
2. **最适合主线的图像母集是 BigEarthNet v2.0 / reBEN 的 Sentinel-1 部分。** 它明确来自 312 景 Sentinel-1 IW GRDH、双极化 VV+VH，包含可追溯命名、10 m GeoTIFF、dB 后向散射、无斑点滤波、地理划分和 CDLA-Permissive-1.0 许可。
3. **最适合小规模“金标准”检查的是 Sentinel2Cap 的人工描述；最适合扩展的是 BigEarthNet.txt。** 前者声明有 12,000 条人工标注描述，后者有 464,044 个 S1/S2 图像对和约 9.6M 文本标注，并提供 1,082 个图像对、15,029 条文本的人工核验基准子集。
4. **SEN12MS 和 SEN1-2 是 GRD 派生切块，不是自然语言数据。** 可以用于图像管线检查或跨数据集泛化，但不能把 MODIS/IGBP 标签或光学配对直接称为“人工 SAR 描述”。SEN1-2 又缺失地理定位且只有 VV，无法满足严格的地理泄漏审计，因此不建议作为主数据。
5. **SAR-TEXT、SARCLIP/SARCAP/MMSAR 仅作外部参考或附加基线。** 已核实的公开说明显示其文本主要由检测框、分割标签、SAR—光学配对等间接监督自动转换；且混合多种传感器，不能默认每个样本都是 Sentinel-1 GRD。代码许可证也不自动覆盖全部上游影像和标注。
6. **现阶段不下载完整大数据。** 先下载/读取元数据、许可证和 8–32 个文件做入口与格式核查；只有在确认能按 `s1_name` 精确取得样本、许可无冲突、哈希可记录后，才进入 1,000 图像级小试验。

本报告用以下标记区分证据状态：`[已核实]` 来自官方页面、论文或数据仓库；`[建议]` 是本课题方案；`[待核实]` 必须在正式使用前补证。

## 2. 三类数据的边界

| 类型 | 判定规则 | 能否直接作为图文检索样本 |
|---|---|---|
| 原始 GRD | Sentinel-1 Level-1 GRD SAFE/ZIP；检测、多视、地距投影，测量值及校准/噪声注记仍在产品内 | 否。需先固定辐射量、几何处理、裁块和文本来源 |
| GRD 派生影像/切块 | 明确记录源产品为 GRD，并已做定标、正射/地形校正、dB 转换或裁块 | 只有配有可追溯文本时才可组成图文样本 |
| SAR—文本数据 | 图像与自然语言或结构化文本成对 | 可以，但必须记录图像源产品、文本来源（人工/模板/模型/标签转写）和许可 |

ESA 对 GRD 的定义是：Level-1 GRD 为聚焦、检测、多视并按 WGS84 椭球投影到地距的 SAR 数据；相位已丢失。参考：[SentiWiki S1 Processing](https://sentiwiki.copernicus.eu/web/s1-processing) 与 [Sentinel-1 Product Definition 2.8](https://sentiwiki.copernicus.eu/__attachments/1673968/S1-RS-MDA-52-7440-Sentinel-1-Product-Definition-2025-2.8.pdf)。

## 3. 已核实资源与筛选结论

### 3.1 原始产品与按需派生入口

| 资源 | 产品、极化与数值 | 已有处理 | 文本 | 许可/入口 | 决策 |
|---|---|---|---|---|---|
| Copernicus Data Space Ecosystem 原始 Sentinel-1 | `[已核实]` Level-1 GRD；IW/SM/EW 可为 VV、HH、VV+VH 或 HH+HV，不能假定全为 VV+VH；原产品测量影像是检测幅度/DN并含校准、噪声等注记 | GRD 形成阶段的检测、多视和地距投影；不是本研究约定的统一 sigma0 dB/RTC 切块 | 无 | Sentinel 数据实行 free, full and open；入口：[CDSE](https://dataspace.copernicus.eu/)；[法律声明](https://sentinels.copernicus.eu/documents/247904/690755/Sentinel_Data_Legal_Notice) | 作为少量溯源/复算样本，不批量下载全景 |
| Google Earth Engine `COPERNICUS/S1_GRD` | `[已核实]` 来源为 Level-1 GRD；波段随产品极化；值为 sigma0 后向散射 dB | 轨道文件、边界噪声、热噪声、辐射定标、SRTM30/ASTER 地形校正、`10log10`；未做 radiometric terrain flattening | 无 | Copernicus 条款；入口：[GEE 数据目录](https://developers.google.com/earth-engine/datasets/catalog/COPERNICUS_S1_GRD)，[处理说明](https://developers.google.com/earth-engine/guides/sentinel1) | 可作小样本按需导出和处理管线对照；必须标为“GEE 派生”而非原始 GRD |

注意：Sentinel Hub 的默认后向散射系数可为 `GAMMA0_ELLIPSOID`，而 GEE `COPERNICUS/S1_GRD` 文档写的是 sigma0 dB。不同入口的默认物理量不能混用；每次请求必须显式记录 `backscatter_coefficient` 和 `units`。参考：[CDSE Sentinel Hub S1GRD](https://documentation.dataspace.copernicus.eu/APIs/SentinelHub/Data/S1GRD.html)。

### 3.2 明确由 GRD 派生的公开切块

| 数据集 | 规模与 GRD 关系 | 极化、单位与处理 | 标签/文本 | 许可与入口 | 适用性 |
|---|---|---|---|---|---|
| BigEarthNet v2.0 / reBEN S1 | `[已核实]` 549,488 个 S1/S2 对；S1 来自 312 景 IW GRDH；每块 1200 m × 1200 m、10 m GSD | VV+VH；精轨、边界/热噪声去除、辐射定标、Range-Doppler 地形校正、线性转 dB；未做斑点滤波 | CLC2018 像素图和 19 类多标签；本体没有自然文本 | CDLA-Permissive-1.0；[主页/下载](https://bigearth.net/)，[v2 说明](https://bigearth.net/static/documents/Description_BigEarthNet_v2.pdf)；S1 全量约 51 GiB | **主图像母集首选**。官方 v2 地理划分优于旧 BigEarthNet 随机/网格划分 |
| SEN12MS | `[已核实]` 180,662 个 S1/S2/MODIS 三元组；S1 为 IW GRD；全球、四季、10 m 对齐 | VV+VH；sigma0 dB；16-bit GeoTIFF；由 GEE/Python/GDAL 半自动制备 | MODIS IGBP/LCCS 地表覆盖图，无自然文本 | CC BY 4.0；[mediaTUM](https://mediatum.ub.tum.de/1474000)，[论文](https://doi.org/10.5194/isprs-annals-IV-2-W7-153-2019)；门户约 510 GB | 图像预处理与跨地域扩展候选；不作为首个图文基线 |
| SEN1-2 | `[已核实]` 282,384 对 S1/S2 对齐切块；全球、四季 | 只提供 S1 VV dB；影像被简化，论文明确不提供地理定位；相邻切块存在高重叠风险 | 无自然文本 | 论文/数据 CC BY 4.0；[官方论文页](https://isprs-annals.copernicus.org/articles/IV-1/141/2018/index.html) | **不建议主用**：缺地理坐标、单极化、无法严格重建防泄漏分组；只可作图像转换或附加泛化 |

### 3.3 SAR—文本数据

| 数据集 | 图像来源 | 文本来源 | 许可与入口 | 决策 |
|---|---|---|---|---|
| Sentinel2Cap | `[已核实]` 仓库样例的 `s1_name` 为 BigEarthNet/reBEN 风格 `S1A_IW_GRDH_1SDV...`；提供 S1 SAR 伪 RGB、S2 RGB/多光谱对应信息 | 12,000 条人工描述，论文称经过人工验证；另有 Qwen3-VL 自动输出，二者必须分开 | [GitHub](https://github.com/LucreziaT/Sentinel2Cap)，[论文](https://arxiv.org/abs/2605.03189)。**许可证文件写数据 CC BY 4.0、代码 MIT，但 README 的表述相反，存在仓库内部冲突** | **小规模金标准首选，但正式使用前须固定提交版本并向作者确认许可冲突；只用人工文本，不混入 Qwen 输出** |
| BigEarthNet.txt | `[已核实]` 464,044 个共注册 S1/S2 图像对，继承 BigEarthNet v2 S1 | 约 9.6M 英文标注；由参考图、地点/季节/气候等属性经模板生成，再做语言自精炼；含 caption、VQA、定位等不同任务。另有 1,082 图像对、15,029 文本的人工核验 benchmark split | CDLA-Permissive-1.0；[项目页](https://txt.bigearth.net/)，[Hugging Face](https://huggingface.co/datasets/BIFOLD-BigEarthNetv2-0/BigEarthNet.txt) | **扩展集首选**。检索只选 `type=captioning`，不得把问答、选择题或框坐标当普通 caption |
| SAR-TEXT | `[已核实]` 超过 130,000 对，汇集检测、分割和 SAR—光学配对等多个既有 SAR 数据源；并非全为 Sentinel-1 GRD | SAR-Narrator 多阶段自动转写，不是统一人工描述 | [官方仓库](https://github.com/YiguoHe/SAR-TEXT)，[论文](https://arxiv.org/abs/2507.18743)。仓库 Apache-2.0 明确覆盖代码，但数据包及上游影像许可边界未在 README 中逐源澄清 | 仅作外部基线/泛化，使用前逐源核许可；不得作为 Sentinel-1 GRD 主真值 |
| SARCLIP 的 SARCAP/MMSAR 等 | `[已核实]` 公开论文说明为多传感器、多分辨率或由检测数据组合 | 结构化标签通过 SARTEX/D2C 转为文本 | 公开论文/仓库；当前未完成逐样本 GRD 与数据许可核验 | 暂不进入主实验，只列相关工作 |

## 4. 推荐的小试验集与扩展集

### 4.1 小试验集 `pilot-v1`

`[建议]` 目标不是追求规模，而是验证数据读取、OpenCLIP 推理、双向检索、规范化消融和可追溯性闭环。

- 图像：1,000 个 BigEarthNet v2 S1 数值切块；仅 `IW_GRDH_1SDV`、VV+VH、10 m、无雪/云阴影标记。
- 文本优先级：
  1. Sentinel2Cap 人工描述中能与上述 `s1_name` 精确匹配的样本；
  2. 若入口/许可尚未确认，则改用 BigEarthNet.txt 人工核验 benchmark 中的 `type=captioning` 记录；
  3. 两者都不可用时，只做 128–256 个自建受控文本样本，并清楚标为“CLC 标签确定性转写 + 人工核验”，不伪称公开人工 caption。
- 数量：训练 600、验证 200、测试 200；若当前阶段只做冻结 OpenCLIP 零样本评测，可先用 200 个独立测试样本。
- 类别：覆盖至少 10 个常见 reBEN 19 类；每类在测试集中至少 20 个正样本。多标签样本保留，不强制单标签化。
- 人工抽检：首个 200 样本全部检查；扩至 1,000 后再分层抽检至少 200 个。
- 下载闸门：先只获取许可证、metadata parquet、文本表和 8–32 个影像文件；确认路径匹配、GeoTIFF 值域、哈希和许可证后才扩大。

### 4.2 扩展集 `extended-v1`

`[建议]`

- 图像母集：BigEarthNet v2 推荐清单中 10,000–50,000 个 S1 切块；保持官方地理 split，另做同源产品/空间邻接泄漏审计。
- 文本：BigEarthNet.txt 只取 captioning 任务；保留 `template/self-refined` 来源，不与人工文本合并汇总。
- 独立泛化：从 SEN12MS 选 2,000–5,000 个地理场景，若需文本，只允许使用单独版本化的规则模板或新人工标注；结果单独报告。
- 人工金标准：Sentinel2Cap 人工描述作为小规模独立测试层，不用于调参。
- SAR-TEXT/SARCLIP 数据：只有在逐源许可、传感器和产品级别审计通过后，才能作为 `external` split；不得并入主训练集后再声称是纯 Sentinel-1 GRD。

## 5. 防地理泄漏划分

### 5.1 分组单位

`[建议]` 每个样本同时生成三个键：

1. `source_product_id`：原 Sentinel-1 GRD 产品/BigEarthNet S1 source tile；
2. `spatial_group_id`：优先使用原 MGRS tile；若有坐标，再附加 20 km 等面积空间块编号；
3. `content_group_id`：对重叠或近重复切块建立连通分量（空间相交、中心距过近或感知哈希高度相似）。

最终 `group_id` 取三类约束的并集连通分量。**同一 `group_id` 只能出现在一个 split。**

### 5.2 划分流程

1. 先删除完全重复文件哈希、重复 `image_id` 和重复 `caption_id`。
2. 用空间索引找出相交切块；不同 split 的切块边界至少留一个完整 patch 宽度（BigEarthNet 为 1.2 km）。保守实验可用 5 km 缓冲带。
3. 将共享原 GRD 产品且空间相邻的切块合并为组；不把相距很远但属于同一超长轨景的样本机械合并，需记录策略。
4. 按组做多标签迭代分层，目标比例 60/20/20（pilot）或 70/10/20（扩展）；不得按单张图随机打散。
5. 优先采用 reBEN 官方地理 split；但仍检查 `source_product_id`、空间相交和文本近重复是否跨 split。
6. 所有文本变体（原文、规范文、模板改写）跟随图像组移动，不能分散到不同 split。
7. 输出 `split_manifest.csv`、每个 split 的 SHA-256、最近跨 split 距离分布和零交叉断言。

### 5.3 必须通过的泄漏断言

- `image_id`、`caption_id`、文件 SHA-256 不跨 split；
- 空间相交面积大于 0 的样本不跨 split；
- 近重复图像对不跨 split；
- 规范文本的源文本不跨 split；
- 同一人工描述不能同时匹配训练和测试图像；
- 报告最小/中位最近跨 split 距离，并列出全部小于缓冲阈值的异常。

## 6. 人工抽检设计

模板见 `templates/stage2_manual_qc.csv`。抽样按 split、国家/区域、季节、CLC 类、轨道方向、入射角区间和文本来源分层。

每条记录至少检查：

- 文件可读、波段/极化与元数据一致；
- NoData、边界噪声、子波束接缝、RFI、饱和、明显错配；
- dB/线性域、sigma0/gamma0 是否与声明一致；
- 图像与文本是否语义一致；文本是否含光学颜色、天气、建筑用途等无证据推断；
- 人工/模板/模型生成来源是否真实记录；
- 地理 split、近重复和同源文本是否泄漏；
- 结论为 `accept / repair / exclude`，并给出严重度和证据说明。

建议两名检查者独立复核至少 50 条重叠样本，报告关键二元项的一致率和 Cohen's kappa；分歧由第三次仲裁解决。

## 7. 双向检索评价

### 7.1 正例定义

- 图找文（I2T）：与该 `image_id` 绑定的全部有效 caption 都是正例。
- 文找图（T2I）：caption 的原始 `image_id` 是主正例；若多个地理不同图像共享同一泛化文本，不能只按字符串相等扩充正例，应另行标注 `semantic_positive_group`。
- 每种方法使用同一测试清单、候选池和正例映射；原文与规范文对照时只替换文本表示，不改变候选图像。

### 7.2 主指标

- I2T `Recall@1/5/10`；
- T2I `Recall@1/5/10`；
- `mR`：上述六个 Recall 的算术平均；
- 每个方向的 `Median Rank` 和 `MRR`。

### 7.3 补充与稳健性

- 多正例场景报告 `mAP@K` 或 `nDCG@K`；
- 按主要地物、国家/空间块、季节、轨道方向、入射角和文本来源分层；宏平均与微平均同时给出；
- 以 `group_id` 为重采样单位做 1,000 次 bootstrap，给出 95% CI；比较规范化前后时报告配对差值 CI；
- 至少 3 个随机种子（若只做冻结推理，则固定排序和并列处理，验证重复运行位级/数值一致性）；
- 额外报告 Top-K 结果稳定性（Jaccard@10）和失败案例，但不以案例代替总体指标。

## 8. 目录与元数据

推荐目录：

```text
data/
  catalog/                 # 数据源、许可、引用、入口；不放影像
  raw/                     # 只读原始文件，保持原名
  interim/                 # 定标/地形校正后的数值栅格
  patches/                 # 冻结裁块
  annotations/
    raw/                   # 原始文本或标签
    normalized/            # 规范文本及版本
  manifests/               # 样本、文件、下载与哈希清单
  splits/v1/               # train/validation/test 与 leakage audit
  qc/                      # 人工抽检、异常和仲裁
  checksums/               # SHA-256
configs/                   # 处理、模型和评价配置
outputs/<run_id>/          # 指标、环境、日志、案例
```

字段字典见 `schemas/stage2_metadata_dictionary.csv`。它补充而不替代现有 `schemas/sample_record.schema.json`。正式实验至少要能从一条样本记录追到：原产品/父数据集、许可、文件哈希、处理流水线、空间分组、原始文本、规范文本和人工审核。

## 9. 下载前闸门

只有同时满足以下条件才允许扩大下载：

- 产品/数据集版本、许可文件和引用已保存；
- 可用 `s1_name` 或产品 ID 反查到影像；
- 8–32 个样本读取成功，极化、尺寸、值域、NoData 与文档一致；
- 文本来源字段非空，人工与自动文本已分开；
- 可生成稳定 SHA-256，下载可断点续传；
- 预计体积、持久化目录和删除策略已确认；
- 地理分组字段足以执行泄漏审计。

## 10. 尚未解决的问题

1. Sentinel2Cap 仓库 README 与专用 `license.txt` 对数据/代码许可证的表述相反；需联系作者或至少固定并存档明确的许可证版本。
2. Sentinel2Cap 的 12,000 条描述与“独立地理图像数、每图描述数、三种模态是否共享同一文本”的精确映射尚未通过文件级小样本验证。
3. BigEarthNet.txt 的人工核验 benchmark split 是否能按单文件随机访问对应 S1 GeoTIFF，尚未实测；官方 BigEarthNet-S1 全量约 51 GiB，不应在此阶段直接整包下载。
4. BigEarthNet/reBEN 的 dB GeoTIFF具体数值编码、NoData 约定和数据类型需以 8–32 文件实测确认。
5. 是否以 `source_product_id` 全景分组，还是只合并同景且空间相邻的切块，会影响样本量与域偏移；应在元数据到手后量化连通分量规模再冻结。

## 11. 主要核验来源

- ESA Sentinel-1 产品定义与处理：[SentiWiki](https://sentiwiki.copernicus.eu/web/s1-processing)
- Copernicus Sentinel 数据法律声明：[Legal Notice](https://sentinels.copernicus.eu/documents/247904/690755/Sentinel_Data_Legal_Notice)
- GEE Sentinel-1 GRD 数据与算法：[目录](https://developers.google.com/earth-engine/datasets/catalog/COPERNICUS_S1_GRD)，[处理步骤](https://developers.google.com/earth-engine/guides/sentinel1)
- BigEarthNet v2.0：[主页](https://bigearth.net/)，[结构与处理说明](https://bigearth.net/static/documents/Description_BigEarthNet_v2.pdf)，[reBEN 论文](https://arxiv.org/abs/2407.03653)
- SEN12MS：[mediaTUM 数据页](https://mediatum.ub.tum.de/1474000)，[论文](https://doi.org/10.5194/isprs-annals-IV-2-W7-153-2019)
- SEN1-2：[论文与入口](https://isprs-annals.copernicus.org/articles/IV-1/141/2018/index.html)
- BigEarthNet.txt：[项目页](https://txt.bigearth.net/)，[数据卡](https://huggingface.co/datasets/BIFOLD-BigEarthNetv2-0/BigEarthNet.txt)
- Sentinel2Cap：[仓库](https://github.com/LucreziaT/Sentinel2Cap)，[论文](https://arxiv.org/abs/2605.03189)
- SAR-TEXT：[仓库](https://github.com/YiguoHe/SAR-TEXT)，[论文](https://arxiv.org/abs/2507.18743)

