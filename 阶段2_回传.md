阶段：
阶段 2——数据与评价

本阶段确定的结论：
- 主图像母集首选 BigEarthNet v2.0 / reBEN 的 Sentinel-1 IW GRDH 双极化切块。
- 小规模人工文本金标准优先 Sentinel2Cap；大规模扩展优先 BigEarthNet.txt；前者许可证表述存在仓库内部冲突，需先确认。
- SEN12MS、SEN1-2 是 GRD 派生图像集而非自然语言集；SAR-TEXT、SARCLIP 类数据为混合来源和自动文本，只作外部参考/基线。
- 正式划分必须以同源产品、空间邻接和内容近重复的并集组为单位，禁止按单图随机切分。

已完成成果及文件位置：
- `阶段2_数据与评价方案.md`
- `schemas/stage2_metadata_dictionary.csv`
- `templates/stage2_manual_qc.csv`
- `阶段2_回传.md`

验证结果：
- 已通过 ESA/Copernicus、GEE、BigEarthNet、mediaTUM、数据集官方仓库与论文核实产品层级、极化、数值域、处理步骤、文本来源、许可与入口。
- 未下载任何大数据；符合“先小规模核查”的约束。
- 已给出可执行的 1,000 图像 pilot、扩展集、下载闸门、地理泄漏断言和双向检索指标。

仍未解决的问题：
- Sentinel2Cap 的许可证冲突及文本—独立图像精确映射需文件级核验。
- BigEarthNet.txt 小基准对应 S1 影像能否按文件精确获取尚未实测。
- BigEarthNet GeoTIFF 的具体 dtype、NoData 和数值范围需对 8–32 文件冒烟检查。
- 最终 group_id 采用整景还是同景空间邻接连通分量，需元数据到手后量化再冻结。

是否需要修改研究主线：
不需要。阶段 2 支持“先以双向检索建立基线，再验证规范化”的主线，但必须把人工文本与自动转写文本分层报告，并把数据可追溯性作为实验前置条件。

建议下一步：
只获取 metadata、许可证和 8–32 个影像/文本样本，验证 `s1_name` 对齐、GeoTIFF 数值域、哈希、人工文本来源和地理分组；通过下载闸门后再生成 1,000 图像 pilot 清单并交给阶段 3 基线运行。
